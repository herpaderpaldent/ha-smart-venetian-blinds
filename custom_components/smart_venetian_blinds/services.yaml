# yaml-language-server: $schema=../../schemas/yaml/services_schema.yaml
# Services for smart_venetian_blinds integration

apply_now:
  name: Apply Now
  description: Force immediate recalculation and application of slat angles to all covers, ignoring throttling rules.
